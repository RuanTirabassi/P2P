"""\nhandlers/hello.py\n\nProcessa mensagens do tipo HELLO recebidas de vizinhos.\n\nComportamento ao receber:\n  1. Registrar peer_id no mapa de peers ativos, associando ao websocket\n  2. Logar a chegada\n\nRef: specs/03-protocolo.md seção HELLO\n"""\n\nfrom src import state\n\n\ndef handle_hello(message: dict, websocket) -> None:\n    """\n    Processa uma mensagem HELLO recebida.\n    Registra o remetente no mapa de peers conectados.\n\n    message   : dict com os campos {type, peer_id, sticker_id, sticker_url}\n    websocket : objeto websocket da conexão com o remetente\n    """\n    peer_id    = message.get(\"peer_id\")\n    sticker_id = message.get(\"sticker_id\")\n\n    if not peer_id:\n        print(\"[HELLO] AVISO: mensagem HELLO sem peer_id — ignorada\")\n        return\n\n    # Evita duplicar o próprio nó na lista de peers\n    if peer_id == state.self_config.get(\"peer_id\"):\n        print(f\"[HELLO] Ignorando HELLO do próprio nó ({peer_id})\")\n        return\n\n    # Registra o peer na memória, associando peer_id ao websocket ativo\n    state.peers[peer_id] = websocket\n    print(f\"[HELLO]  Peer {peer_id} registrado com sucesso (sticker: {sticker_id})\")\n