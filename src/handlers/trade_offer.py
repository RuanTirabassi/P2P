"""\nhandlers/trade_offer.py\n\nProcessa mensagens do tipo TRADE_OFFER recebidas.\nUma proposta de troca de figurinha chegou de outro nó.\n\nFluxo de decisão:\n  1. Verificar se to_peer == próprio peer_id\n  2. Verificar se possuo a figurinha pedida (want) com qty >= 1\n  3. Se sim → enviar TRADE_ACCEPT + executar a troca localmente\n  4. Se não → enviar TRADE_REJECT com motivo\n\nRef: specs/03-protocolo.md seção TRADE_OFFER\nRef: specs/04-regra-de-negocio.md seção Troca\n"""\n\nimport json\nfrom datetime import datetime, timezone\nfrom src import state\nfrom src.inventory import has_sticker, decrement_sticker, increment_sticker\n\n\ndef handle_trade_offer(message: dict, websocket) -> None:\n    """\n    Processa uma mensagem TRADE_OFFER recebida.\n\n    message   : dict com {type, trade_id, from_peer, to_peer, offer, want}\n    websocket : websocket do vizinho que enviou a mensagem\n    """\n    trade_id  = message.get(\"trade_id\")\n    from_peer = message.get(\"from_peer\")\n    to_peer   = message.get(\"to_peer\")\n    offer     = message.get(\"offer\")\n    want      = message.get(\"want\")\n    my_peer_id = state.self_config.get(\"peer_id\")\n\n    print(f\"[TRADE]  Proposta recebida de {from_peer}: oferece {offer}, quer {want}\")\n\n    # Verifica se a proposta é realmente para este nó\n    if to_peer != my_peer_id:\n        print(f\"[TRADE]  AVISO: destinado a {to_peer}, mas sou {my_peer_id} — ignorando\")\n        return\n\n    # Verifica se a figurinha solicitada está disponível no inventário local\n    if not has_sticker(state.inventory, want):\n        reason = f\"Figurinha {want} indisponível no inventário\"\n        print(f\"[TRADE]  Rejeitando proposta de {from_peer}: {reason}\")\n        _send_to_peer(from_peer, {\"type\": \"TRADE_REJECT\", \"trade_id\": trade_id,\n                                  \"from_peer\": my_peer_id, \"to_peer\": from_peer, \"reason\": reason})\n        return\n\n    # Registra a negociação em andamento antes de aceitar\n    state.pending_trades[trade_id] = {\"from_peer\": from_peer, \"to_peer\": to_peer, \"offer\": offer, \"want\": want}\n    _send_to_peer(from_peer, {\"type\": \"TRADE_ACCEPT\", \"trade_id\": trade_id,\n                              \"from_peer\": my_peer_id, \"to_peer\": from_peer})\n\n    # === EXECUÇÃO DA TROCA ===\n    # Ordem importa: decrementar primeiro, incrementar depois, salvar por último.\n    try:\n        if not has_sticker(state.inventory, want):\n            raise ValueError(f\"{want} ficou indisponível entre o aceite e a execução\")\n\n        # Passo 1: remove 1 unidade da figurinha que este nó está cedendo\n        decrement_sticker(state.inventory, want)\n\n        # Passo 2: adiciona 1 unidade da figurinha recebida em troca\n        increment_sticker(state.inventory, offer)\n\n        # Passo 3: envia TRANSFER_CONFIRM\n        _send_to_peer(from_peer, {\"type\": \"TRANSFER_CONFIRM\", \"trade_id\": trade_id,\n                                  \"from_peer\": my_peer_id, \"gave\": want, \"received\": offer})\n\n        # Passo 4: registra no histórico\n        state.trade_history.append({\"trade_id\": trade_id,\n            \"timestamp\": datetime.now(timezone.utc).isoformat(),\n            \"partner\": from_peer, \"gave\": want, \"received\": offer})\n        del state.pending_trades[trade_id]\n\n        print(f\"[TRADE]  Troca concluída — dei {want}, recebi {offer}\")\n        print(f\"[INVENTÁRIO] {offer}: {state.inventory.get(offer, 0)} | {want}: {state.inventory.get(want, 0)}\")\n\n    except ValueError as e:\n        print(f\"[TRADE]  ERRO na execução da troca: {e}\")\n        _send_to_peer(from_peer, {\"type\": \"TRADE_REJECT\", \"trade_id\": trade_id,\n                                  \"from_peer\": my_peer_id, \"to_peer\": from_peer, \"reason\": str(e)})\n\n\ndef _send_to_peer(peer_id: str, msg: dict) -> None:\n    \"\"\"Envia uma mensagem JSON para um peer específico pelo peer_id.\"\"\"\n    if peer_id in state.peers:\n        try:\n            state.peers[peer_id].send(json.dumps(msg, ensure_ascii=False))\n        except Exception as e:\n            print(f\"[TRADE]  Erro ao enviar para {peer_id}: {e}\")\n    else:\n        print(f\"[TRADE]  AVISO: {peer_id} não está nos peers conectados\")\n